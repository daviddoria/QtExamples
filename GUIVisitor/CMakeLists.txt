cmake_minimum_required(VERSION 2.6)

PROJECT(GUIVisitor)

FIND_PACKAGE(Qt4 REQUIRED)
INCLUDE(${QT_USE_FILE})

QT4_WRAP_CPP(MOCSrcs DrawVisitor.h MainLoopObject.h)
ADD_EXECUTABLE(GUIVisitor main.cpp ${MOCSrcs})
TARGET_LINK_LIBRARIES(GUIVisitor ${QT_LIBRARIES})
