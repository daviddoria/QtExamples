cmake_minimum_required(VERSION 2.6)

PROJECT(Debug)
FIND_PACKAGE(Qt4 REQUIRED)
INCLUDE(${QT_USE_FILE})

include_directories(${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})

ADD_EXECUTABLE(Debug Debug.cpp)
TARGET_LINK_LIBRARIES(Debug ${QT_LIBRARIES})

